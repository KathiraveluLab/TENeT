# TENeT Development Environment
# Copy this file to .env and adjust as needed
#   cp .env.example .env

# Ports (change if defaults conflict with other services)
BACKEND_PORT=5001
FRONTEND_PORT=5173

# Flask
FLASK_DEBUG=1

# Database (sqlite is default, no extra config needed)
DB_TYPE=sqlite

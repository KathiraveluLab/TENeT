# Python bytecode
__pycache__/
*.py[cod]
*$py.class

# Local virtual environments
.venv/
venv/
ENV/
env/
alaska_all_data
# Local databases (don't copy your local SQLite to the container)
*.sqlite3
db.sqlite3

# Git and OS metadata
.git
.gitignore
.DS_Store

# Docker files (not needed inside the image)
Dockerfile
docker-compose.yml
.dockerignore

# Sensitive information
.env